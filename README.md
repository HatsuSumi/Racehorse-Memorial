# 去世的现役与退役赛马名录 (Racehorse Memorial Directory)

> "每一匹马都曾在赛场上留下过足迹，每一段生命都值得被记录。"

这是一个专注于记录和纪念年度去世赛马的纪念网站。我（没有们）希望通过这个简洁、静谧的页面，为每一位赛马爱好者提供一个查阅和缅怀的空间。

**🌐 在线访问**：[https://hatsusumi.github.io/Racehorse-Memorial/](https://hatsusumi.github.io/Racehorse-Memorial/)

- **当前收录年份**：2025年
- **收录范围**：全球范围内的现役与退役赛马，无论是不是名马都收录，有被媒体报道的，能被搜索引擎搜索到的赛马。

## ✨ 功能特性 (Features)

### 📊 数据阅览
- **多维度名录**：详细收录马名、生卒年、战绩、奖金、血统等信息。
- **智能统计**：自动识别并在表格中标记重复出现的父系/母系马匹及逝世地（实时角标计数）。
- **图片画廊**：集成 Lightbox 查看大图。

### 🔍 深度搜索系统
这不是一个简单的文本过滤，而是一个全功能的搜索引擎：
- **高级匹配**：支持全词匹配、忽略大小写、**忽略平/片假名差异**（针对日语马名优化）。
- **正则支持**：支持正则表达式搜索。
- **交互导航**：
  - 搜索结果高亮，并支持使用 `Enter` / `↑` / `↓` 键在结果间快速跳转和平滑滚动。
  - **输入法优化**：智能识别 IME 输入状态，防止中文输入过程中频繁触发搜索。
> 浏览器原生的 `Ctrl+F`？不存在的，我选择重造轮子！

### 🛠 交互与工具
- **多维排序**：
  - **智能奖金排序**：内置汇率换算逻辑，将不同货币统一基准进行排序。
  - **战绩深度分析**：点击表头唤起专用面板，支持按胜率、连对率、出赛数等 **8 种维度**排序，并在单元格中动态展示对应指标的百分比/数值 Badge。
- **暗色模式**：内置“拥抱光明 / 堕入黑暗”主题切换，持久化保存用户偏好。
- **智能响应式**：
  - 完美适配移动端，包含侧边栏导航。
  - **动态布局**：搜索框会根据屏幕宽度，在侧边栏与主内容区之间自动“搬家”，保证在任何设备上都触手可及。
- **配置驱动可视化**：`viz.html` 仪表盘完全由配置驱动，支持分年份定义不同的图表组合（如死因分布、殒命赛场等），无需修改代码即可扩展。

## 🏗 技术栈 (Tech Stack)

本项目坚持 **"No Framework"** 理念，追求极致的加载速度和运行效率：

- **核心**：原生 HTML5 / CSS3 / JavaScript (ES6+ Modules)。
- **架构**：
  - 采用 **ES Modules** 进行模块化开发，职责分离（Services, Controllers, Renderers）。
  - **无任何第三方运行时依赖**，完全手写路由逻辑、模态框和 Tooltip。
- **性能优化**：
  - 使用 `TreeWalker` API 实现高性能 DOM 文本节点搜索。
  - 使用 `DocumentFragment` 最小化重排与重绘。
  - 数据层实现内存缓存。

## 📂 项目结构

```text
Racehorse-Memorial/
├── start.bat                   # 启动本地服务器并打开浏览器 (8080)
├── project_stats.py            # 项目规模/资源统计脚本（文件数/代码行/字符等）
├── viz.html                    # 数据可视化页面（ECharts）
├── css/
│   ├── components/
│   │   ├── modal.css           # 模态框样式
│   │   ├── references.css      # 参考文献区域样式
│   │   └── table.css           # 数据表格核心样式
│   ├── core/
│   │   ├── base.css            # 基础重置与排版
│   │   ├── layout.css          # 整体布局
│   │   └── tokens.css          # CSS 变量定义 (颜色、间距)
│   ├── features/
│   │   ├── search.css          # 搜索框与高亮样式
│   │   ├── tooltip.css         # Tooltip 样式
│   │   └── viz.css             # 数据可视化页面样式
│   ├── responsive/
│   │   └── responsive.css      # 移动端适配
│   └── style.css               # 样式入口文件
├── data/
│   └── 2025.json               # 2025年数据源
├── favicon/                    # 站点图标
├── images/                     # 马匹图片资源 (按年份分类)
├── js/
│   ├── config/
│   │   └── config.js           # 全局配置 (表头定义、汇率表)
│   ├── controllers/
│   │   ├── exchange-rate-controller.js   # 汇率数据更新控制
│   │   ├── mobile-menu.js                # 移动端侧边栏逻辑
│   │   ├── quick-nav.js                  # 快速导航 (顶部/底部)
│   │   ├── responsive-search-placement.js # 搜索框位置自适应
│   │   ├── sort-controller.js            # 表格排序协调器
│   │   └── theme-controller.js           # 主题切换逻辑
│   ├── features/
│   │   ├── search/
│   │   │   └── search.js                 # 搜索核心算法与交互
│   │   ├── sort/
│   │   │   ├── record-sort-modal.js      # 战绩排序弹窗
│   │   │   └── sort.js                   # 排序算法实现
│   │   └── tooltip/
│   │       ├── tooltip-theme.js          # Tooltip 主题适配
│   │       └── tooltip.js                # Tooltip 核心逻辑
│   ├── renderers/
│   │   ├── sort-hint-renderer.js         # 排序提示渲染
│   │   └── ui-renderer.js                # DOM 操作与页面渲染
│   ├── services/
│   │   ├── data-service.js               # 数据获取与缓存
│   │   └── exchange-rate-service.js      # 汇率 API 服务
│   ├── shared/
│   │   ├── storage.js          # LocalStorage 封装
│   │   └── utils.js            # 通用工具函数
│   ├── pages/
│   │   └── viz.js              # 可视化页面逻辑
│   └── app.js                  # 应用入口与初始化
├── tools/
│   ├── convert.py              # Excel/CSV 转 JSON 工具
│   ├── compare_json_xlsx.py    # JSON 与 XLSX 数据对比工具
│   ├── rename_images.py        # 根据 JSON 序号重命名图片工具
│   └── 2025 Racehorse Deaths (Active & Retired).xlsx  # 原始数据
├── index.html                  # 页面入口
└── README.md                   # 项目说明
```

## 📊 项目规模

### 文件统计

- **总文件数**：39 个
  - JavaScript 文件：20 个
  - CSS 文件：11 个
  - Python 脚本：3 个
  - HTML 文件：2 个
  - JSON 数据：1 个
  - Markdown 文档：1 个
  - 批处理脚本：1 个

### 代码规模

- **代码总行数**：7,802 行（不含空行、注释）
  - JavaScript：3,230 行（41.4%）
  - JSON：2,264 行（29.0%）
  - CSS：1,469 行（18.8%）
  - Python：485 行（6.2%）
  - HTML：291 行（3.7%）
  - Batch：63 行（0.8%）

- **字符总数**：256,255 字符（不含注释）
  - JavaScript：133,818 字符（52.2%）
  - JSON：61,256 字符（23.9%）
  - CSS：33,419 字符（13.0%）
  - Python：13,809 字符（5.4%）
  - HTML：12,500 字符（4.9%）
  - Batch：1,453 字符（0.6%）

> 💡 **数据来源**：以上数据基于项目内的 `project_stats.py` 统计生成
> 
> 📊 **统计工具 GitHub 仓库链接**：[Omni-Project-Stats](https://github.com/HatsuSumi/Omni-Project-Stats) —— 90%项目通用，涵盖多个领域

## 🚀 数据维护

数据源维护在 `tools/` 目录下的 Excel 或 CSV 文件中。

1. 修改 Excel/CSV 文件中的数据。
2. 运行 `tools/convert.py` 生成最新的 JSON 数据。
3. 确保 `js/config/config.js` 中的年份配置已更新。

---

*May they rest in green pastures.*
